const config = {
    autoUpdate: {
        enabled: true,
        allowPrerelease: false,
    },
    window: {
        defaultWidth: 800,
        defaultHeight: null,
        minWidth: 600,
        minHeight: 600,
    },
    tray: {
        enabled: true,
        tooltip: 'Duksu'
    }
};

export default config;